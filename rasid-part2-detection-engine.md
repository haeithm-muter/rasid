# رصيد (Rasid) — الجزء 2 من 3: محرك الكشف

> هذا الجزء الثاني من مشروع "رصيد". الجزء الأول (سبق بناؤه في جلسة سابقة) أنتج:
> - `src/core/` ببنية بيانات `Transaction` موحّدة
> - `src/adapters/` بأربعة محوّلات بنكية + كاشف تلقائي للتنسيق
> - `src/core/merchantNormalizer.ts` لتوحيد أسماء التجار وتجميعها
>
> **قبل أي شيء: افتح المشروع الموجود واقرأ `src/core` و `src/adapters` بالكامل لتفهم البنية والأنواع الموجودة فعلياً قبل الكتابة.**
> في هذه الجلسة نبني فقط: محرك الكشف الرياضي (قلب المنتج). **لا تبني الواجهة (UI) ولا تلمس مرحلة التقييم الكاملة (eval) في هذه الجلسة** — تلك للجزء الثالث.

---

## 1. نظرة عامة على المشروع الكامل (للسياق فقط)

رصيد يكشف الاشتراكات المنسية من كشف حساب بنكي، بما فيها الدورات غير المنتظمة (كل 28 يوماً تنزلق عبر الشهور)، وزيادات الأسعار الصامتة، وتحوّل التجارب المجانية. كل شيء يعمل محلياً داخل المتصفح.

---

## 2. المطلوب بناؤه في هذه الجلسة بالتحديد

ابنِ في `src/core/detectionEngine.ts` (أو ملفات فرعية منظمة داخل `src/core/`) الدوال التالية، كلها **دوال نقية pure functions** بلا أي تبعية على React أو المتصفح:

### 2.1 كشف الدورية (periodicity detection)
- لكل مجموعة تاجر بها **3 عمليات أو أكثر** (المجموعات تأتي من `merchantNormalizer` المبني في الجزء الأول)
- احسب الفروق الزمنية بالأيام بين كل عمليتين متتاليتين مرتبة زمنياً
- استخدم **الوسيط (median)** لهذه الفروق، **وليس المتوسط (average)** — لأنه أقوى ضد القيم الشاذة
- صنّف الدورة حسب الوسيط باستخدام هذا الجدول بالضبط:

| الفرق بالأيام (الوسيط) | التصنيف |
|---|---|
| 6–8 | أسبوعي |
| 13–16 | نصف شهري |
| 27–32 | شهري |
| 85–95 | ربع سنوي |
| 175–190 | نصف سنوي |
| 355–375 | سنوي |

- إذا لم يقع الوسيط ضمن أي مدى، صنّفها "غير منتظم/غير مصنّف" بدل إجبارها على تصنيف خاطئ

> **متطلب حرج جداً — اختبره جيداً:** يجب أن يُكشف اشتراك بدورة كل 28 يوماً بالضبط بنفس القوة، حتى لو كان تاريخ الخصم "ينزلق" عبر أيام الشهر المختلفة (مثلاً: 1 يناير، 29 يناير، 26 فبراير...). الخوارزمية يجب ألا تعتمد إطلاقاً على "نفس رقم اليوم من كل شهر" — هذا بالتحديد ما تفشل فيه الأدوات البسيطة، وهو أهم نقطة تمايز في المنتج.

- احسب أيضاً **درجة "انتظام" الدورة** من مدى تشتت الفروق حول الوسيط (كلما قلّ التشتت، زاد الانتظام)

### 2.2 ثبات المبلغ (amount stability)
- لكل مجموعة تاجر، احسب مدى تشتت المبالغ
- الاشتراك الحقيقي غالباً مبلغه ثابت أو شبه ثابت (باستثناء حالات زيادة السعر، انظر أدناه)

### 2.3 درجة الثقة (confidence score)
- ادمج: انتظام الدورة + ثبات المبلغ + عدد العمليات في مجموعة واحدة
- أخرج رقماً بين 0 و 1
- وثّق بوضوح كيف تُدمج هذه العوامل الثلاثة (الصيغة والأوزان) في تعليق أعلى الدالة

### 2.4 كشف زيادة السعر
- عندما تنقسم المبالغ داخل مجموعة تاجر واحدة إلى **مستويين واضحين أو أكثر** بحدّ زمني واضح (المبلغ كان X ثم أصبح Y بعد تاريخ معين ولم يعد Y)
- أخرج: نسبة الزيادة + تاريخ حدوثها التقريبي

### 2.5 كشف تحوّل التجربة المجانية
- عندما يكون **أول رسم في المجموعة = صفر، أو أقل من 25% من المبلغ الثابت اللاحق**
- ضع علامة/وسم تنبيه على هذه المجموعة

### 2.6 درجة الاشتباه (suspicion score) لترتيب النتائج
- رتّب المجموعات حسب: مدة الاستمرار (الأقدم أعلى شكاً) + صغر المبلغ (الأصغر أعلى شكاً) + ثبات النمط
- المنطق: المبالغ الصغيرة القديمة الثابتة هي الأكثر عرضة لأن يُنسى أمرها من صاحبها

---

## 3. التمييز المطلوب (نقطة خطر معروفة — انتبه)

لا تصنّف كل عملية دورية كاشتراك. مثالان يجب أن يفشل المحرك في تصنيفهما كاشتراك:
- **إيجار شهري ثابت**: دوري ومبلغه ثابت، لكنه ليس اشتراكاً برمجياً — إذا لم يكن هناك طريقة منطقية للتمييز ضمن هذا الجزء، اكتب ملاحظة صريحة في الكود توضح أن هذا قرار متروك لعتبة/قاعدة تُضبط لاحقاً في التقييم، ولا تخفِ المشكلة
- **بقالة أسبوعية بمبالغ متغيّرة**: دورية لكن مبلغها غير ثابت إطلاقاً — يجب أن يفشل اختبار ثبات المبلغ هنا تلقائياً ويستبعدها

---

## 4. الاختبارات المطلوبة في هذه الجلسة

اكتب اختبارات vitest شاملة (بيانات اصطناعية بسيطة تكتبها أنت داخل الاختبار، وليست ملفات CSV) لـ:
- تصنيف كل مدى من مدايات الجدول أعلاه (أسبوعي، نصف شهري، شهري، ربع سنوي، نصف سنوي، سنوي)
- **اختبار خاص ومؤكد لحالة الدورة كل 28 يوماً المنزلقة عبر الشهور** — هذا الاختبار غير قابل للتفاوض
- ثبات المبلغ مع مبالغ ثابتة تماماً، شبه ثابتة، ومتذبذبة تماماً
- كشف زيادة السعر بمستويين وأكثر
- كشف تحوّل التجربة المجانية
- حالة الإيجار الثابت وحالة البقالة المتغيّرة (وثّق في تعليق الاختبار أن هذه "حالات حدية معروفة" وليست فشلاً كاملاً إن لم تُستبعد تماماً بعد)
- حالات حدية: مجموعة بأقل من 3 عمليات (يجب ألا تُصنَّف دورية أصلاً)، مجموعة بعمليتين فقط، عملية واحدة يتيمة

---

## 5. معايير القبول لهذه الجلسة تحديداً

- [ ] `npm run test` ناجح بالكامل (اختبارات الجزء الأول + اختبارات هذا الجزء)
- [ ] `npm run build` يعمل بدون أخطاء TypeScript
- [ ] كل دوال محرك الكشف في `src/core/`، نقية 100%، بلا أي استيراد React
- [ ] اختبار الدورة كل 28 يوماً المنزلقة ناجح تحديداً واسمه واضح يدل على أنه يختبر هذه الحالة بالذات
- [ ] commits منظمة بعد كل دالة رئيسية تكتمل، لا commit ضخم واحد
- [ ] **لا كود واجهة (UI) ولا سكربت تقييم كامل في هذه الجلسة**

---

## 6. ملاحظة لكلود كود

الجلسة القادمة (الجزء الثالث) ستبني الواجهة ومرحلة التقييم الكمّي فوق ما تبنيه الآن، بذاكرة منفصلة تماماً عن هذه الجلسة. لذلك: وثّق كل دالة تصدرها بتعليق يشرح المدخلات والمخرجات وأمثلة صغيرة إن أمكن، والتزم بالبنية والأسماء الموجودة فعلياً من الجزء الأول بدل إعادة تسميتها.
