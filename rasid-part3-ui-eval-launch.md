# رصيد (Rasid) — الجزء 3 من 3: الواجهة + التقييم الكمّي + النشر

> هذا الجزء الأخير والثالث من مشروع "رصيد". في جلستين سابقتين تم بناء:
> - `src/core/` و `src/adapters/`: نموذج بيانات، 4 محوّلات بنكية، كاشف تلقائي، توحيد أسماء التجار (الجزء 1)
> - `src/core/detectionEngine.ts`: كشف الدورية، ثبات المبلغ، درجة الثقة، زيادة السعر، تحوّل التجربة المجانية، درجة الاشتباه (الجزء 2)
>
> **قبل أي شيء: افتح المشروع الموجود بالكامل، اقرأ `src/core` و `src/adapters` والاختبارات الموجودة، وشغّل `npm run test` للتأكد أن كل شيء سابق لا يزال ناجحاً قبل إضافة أي كود جديد.**

---

## 1. نظرة عامة على المشروع الكامل (للسياق فقط)

رصيد يقرأ كشف حساب بنكي (CSV) داخل المتصفح فقط، ويكشف الاشتراكات المنسية وزيادات الأسعار الصامتة وتحوّل التجارب المجانية. **صفر رفع بيانات لأي خادم — هذا الشرط غير قابل للتفاوض.**

---

## 2. المطلوب بناؤه في هذه الجلسة — أربعة أجزاء

### 2.1 الواجهة (`src/ui/`)

صفحة واحدة، **ثلاث حالات**:

1. **الاستقبال (Upload)**
   - منطقة سحب وإفلات لملف CSV
   - جملة واحدة تشرح وظيفة التطبيق
   - سطر بارز وواضح: "ملفك لا يغادر جهازك — لا يوجد خادم"
   - زر "جرّب ببيانات تجريبية" يشغّل التطبيق ببيانات مولّدة محلياً دون أي رفع ملف حقيقي

2. **المعالجة (Processing)**
   - شريط تقدّم
   - **المعالجة يجب أن تتم داخل Web Worker** لتفادي تجميد واجهة المستخدم أثناء المعالجة

3. **النتائج (Results)**
   - بطاقات علوية: الكلفة الشهرية الإجمالية، الكلفة السنوية المتوقعة، عدد الاشتراكات المكتشفة
   - جدول اشتراكات بالأعمدة: التاجر، المبلغ، الدورة، أول ظهور، آخر خصم، درجة الثقة، وسم "زيادة سعر" أو "تجربة تحوّلت" عند وجودها
   - **الضغط على أي صف يعرض العمليات الأصلية** التي بُني عليها الاستنتاج — هذه ميزة ثقة أساسية، المستخدم يتحقق بنفسه من صحة الكشف
   - رسم بسيط بأعمدة CSS (بدون مكتبة رسوم بيانية) لتوزيع الكلفة الشهرية بين التجار
   - زر "تصدير قائمة الإلغاء" يُصدّر قائمة الاشتراكات كملف نصي (txt) قابل للتحميل

**متطلبات غير وظيفية للواجهة:**
- دعم كامل للعربية والإنجليزية، **RTL كامل**، وكل النصوص مجمّعة في ملف واحد قابل للترجمة (لا نصوص متفرقة داخل المكوّنات)
- ألوان درجة الثقة (عالية/متوسطة/منخفضة) يجب ألا تعتمد على اللون وحده (أضف أيقونة أو نص أو نمط مختلف لدعم ضعاف الرؤية اللونية)
- الواجهة تعمل بشكل جيد على شاشة الهاتف (تصميم متجاوب)

### 2.2 التحقق من الخصوصية (غير قابل للتفاوض)

بعد بناء الواجهة، تحقق فعلياً (اذكر كيف تحققت في رسالة الـ commit أو في تعليق) أن تبويب Network في المتصفح **فارغ تماماً** بعد رفع ملف — صفر طلبات شبكة، لا رفع، لا API، لا تحليلات (analytics)، لا خطوط خارجية (استخدم خطوطاً محلية أو من النظام)، لا أي تتبّع من أي نوع.

### 2.3 التقييم الكمّي (`eval/`)

هذا الجزء **لا يُحذف مهما ضاق الوقت** — بدونه المشروع بلا دليل كمّي على فعاليته.

- **مولّد بيانات اصطناعية**: يولّد 12 كشف حساب بحقيقة أرضية معروفة (تعرف أنت أي عملية هي اشتراك فعلاً)، موزّعة على الـ 5 تنسيقات البنكية (استخدم تنسيقات الجزء الأول)، كل كشف يغطي 6–12 شهراً بين 80–300 عملية. كل كشف يجب أن يتضمن:
  - اشتراكات شهرية ثابتة
  - اشتراكاً بدورة 28 يوماً متزحزحة (نفس الحالة الحرجة من الجزء الثاني)
  - اشتراكاً سنوياً
  - زيادة سعر في المنتصف
  - تجربة مجانية متحوّلة إلى مدفوعة
  - اشتراكاً أُلغي في المنتصف (توقف الخصم)
  - ضوضاء واقعية: مشتريات عشوائية، بقالة أسبوعية متغيّرة المبلغ، إيجار شهري ثابت (حالة صعبة عمداً)
  - أخطاء واقعية: صفوف مكررة، أوصاف ناقصة، مبالغ مستردة (refunds)

- **سكربت قياس** (`eval/measure.ts` أو مشابه) يُشغَّل عبر أمر npm منفصل، ويحسب ويطبع:
  - recall و precision (الهدف: كلاهما ≥ 90%)
  - دقة تصنيف نوع الدورة
  - دقة كشف زيادات الأسعار
  - دقة كشف التجارب المتحوّلة
  - زمن المعالجة لكل 1000 عملية
  - جدول بالحالات الفائتة (false negatives) وسببها التقريبي

- يُولَّد تلقائياً ملف `eval/RESULTS.md` بتاريخ التشغيل والأرقام الفعلية

**قاعدة صارمة: لا تكتب أي رقم أداء (recall %, precision %) في أي مكان في المشروع أو README قبل تشغيل هذا السكربت فعلياً والحصول على الرقم الحقيقي.**

### 2.4 التوثيق والنشر

- **README.md** بهذا الترتيب بالضبط:
  1. المشكلة
  2. الفجوة في الحلول الحالية
  3. آلية العمل (مخطط Mermaid يوضح تدفق البيانات: رفع → تحليل → توحيد → كشف → نتائج)
  4. النتائج الفعلية (من `eval/RESULTS.md` فقط — أرقام حقيقية لا تقديرية)
  5. الخصوصية (صفر شبكة، صفر خادم)
  6. قسم صريح بعنوان "ما لا يفعله هذا المشروع" (PDF، ربط بنكي، تطبيق هاتف، إلغاء تلقائي، تصنيف بذكاء اصطناعي، حسابات مستخدمين)
  7. القرارات المعمارية (لماذا Web Worker، لماذا Levenshtein محلي، لماذا وسيط لا متوسط)
  8. التشغيل محلياً (خطوات npm install / npm run dev)

- **`docs/WALKTHROUGH.md`**: شرح تعليمي كامل ملفاً بملف — يشرح كل ملف رئيسي في المستودع بجملة أو جملتين، بحيث يقدر أي شخص (حتى بدون خلفية برمجية عميقة) يفهم بنية المشروع كاملة بقراءته

- **CI**: إعداد GitHub Actions يشغّل الاختبارات + البناء تلقائياً على كل push
- **نشر تلقائي على GitHub Pages** عند نجاح CI على الفرع الرئيسي

---

## 3. الاختبارات المطلوبة في هذه الجلسة

- المجموع الكلي للمشروع (كل الأجزاء الثلاثة) يجب أن يتجاوز **30 اختباراً ناجحاً**
- تأكد أن `npm run test` و `npm run build` ناجحان بالكامل قبل أي commit نهائي
- اختبر يدوياً (أو صف كيف اختبرت) زر "بيانات تجريبية" وتأكد أنه يعمل دون أي رفع ملف حقيقي

---

## 4. معايير القبول النهائية للمشروع كاملاً (Definition of Done)

- [ ] رابط حي يعمل على GitHub Pages (اختبره من نافذة تصفح خفي/incognito)
- [ ] GIF توضيحي في أعلى README يوضح الاستخدام
- [ ] زر "بيانات تجريبية" يعمل دون رفع أي ملف
- [ ] تبويب Network فارغ تماماً بعد رفع ملف حقيقي
- [ ] أكثر من 30 اختباراً ناجحاً + CI أخضر على GitHub
- [ ] رقم أداء حقيقي واحد على الأقل في README + سكربت التقييم موجود وقابل للتشغيل في المستودع
- [ ] قسم "ما لا يفعله هذا المشروع" موجود بوضوح في README
- [ ] **لا يوجد أي كشف حساب حقيقي في المستودع أو في تاريخ الـ commits** (تحقق يدوياً قبل آخر push)
- [ ] `docs/WALKTHROUGH.md` مكتوب بالكامل ويغطي كل ملف رئيسي
- [ ] تاريخ commits نظيف برسائل واضحة ومفهومة (لا "fix" أو "update" بلا سياق)
- [ ] كل ملف في المستودع يمكن شرح وظيفته بجملة واحدة دون العودة للكود (بند غير قابل للتفاوض)

---

## 5. ملاحظة أخيرة لكلود كود

هذه آخر جلسة في المشروع. بعد إتمام كل ما سبق، راجع المشروع كاملاً من البداية (الأجزاء الثلاثة مجتمعة) وتأكد أن كل بند في قائمة "معايير القبول النهائية" أعلاه محقق فعلياً، لا افتراضياً. إذا وجدت بنداً غير محقق، أصلحه قبل اعتبار المشروع مكتملاً.
