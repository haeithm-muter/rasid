# رصيد (Rasid) — الجزء 1 من 3: التأسيس وطبقة الإدخال

> هذا أول جزء من مشروع "رصيد" — كاشف الاشتراكات المنسيّة من كشف الحساب البنكي.
> نبني في هذا الجزء فقط: هيكل المشروع، قراءة CSV، محوّلات البنوك، وتوحيد أسماء التجار.
> **لا تبني الواجهة (UI) ولا محرك الكشف الرياضي في هذه الجلسة** — هذول أجزاء لاحقة منفصلة.

---

## 1. نظرة عامة على المشروع الكامل (للسياق فقط)

رصيد تطبيق ويب يقرأ كشف حساب بنكي (CSV) داخل المتصفح فقط، ويكشف الرسوم المتكررة المنسية (اشتراكات)، بما فيها الدورات غير المنتظمة (كل 28 يوماً)، وزيادات الأسعار الصامتة، وتحوّل التجارب المجانية إلى مدفوعة. **صفر رفع بيانات لأي خادم.**

---

## 2. الحزمة التقنية

- Vite + React + TypeScript (`strict: true`) + vitest
- الاعتمادية الوحيدة المسموحة: `papaparse` (تحليل CSV)
- بلا أي مكتبة رسوم بيانية أو مكتبات إضافية غير ضرورية

## 3. بنية المجلدات المطلوبة

```
src/core/     — منطق خالص، بلا React إطلاقاً
src/ui/       — (فارغ الآن، يُبنى لاحقاً في الجزء 3)
src/adapters/ — محوّلات تنسيقات البنوك
tests/
eval/         — (فارغ الآن، يُبنى لاحقاً)
```

**قاعدة صارمة:** كل منطق في `src/core/` و `src/adapters/` يجب أن يكون دوال نقية (pure functions) قابلة للاختبار بمعزل تام عن المتصفح — بدون أي استيراد لـ React.

---

## 4. المطلوب بناؤه في هذه الجلسة بالتحديد

### 4.1 تهيئة المشروع
- إنشاء مشروع Vite + React + TypeScript جديد
- تفعيل `strict: true` في tsconfig
- تثبيت `papaparse` و `vitest`
- إعداد سكربت اختبار (`npm run test`)

### 4.2 نموذج البيانات الموحّد
عرّف نوع TypeScript موحّد لكل عملية بنكية:
```ts
type Transaction = {
  date: string;
  description: string;
  amount: number;
  currency: string;
  direction: "debit" | "credit";
};
```

### 4.3 واجهة BankAdapter + 4 محوّلات
- عرّف واجهة (interface) موحّدة اسمها `BankAdapter` تحوّل صفوف CSV خام إلى `Transaction[]`
- ابنِ **4 محوّلات** لتنسيقات بنكية مختلفة (اختر تنسيقات واقعية شائعة، عربي وإنجليزي)، تغطي:
  - أسماء أعمدة مختلفة (عربي/إنجليزي)
  - صيغ تاريخ مختلفة: `DD/MM/YYYY`، `MM/DD/YYYY`، `YYYY-MM-DD`
  - تمثيل مبلغ بعمود واحد بإشارة، أو عمودين منفصلين (مدين/دائن)
  - فواصل الآلاف ورموز العملة داخل الخلية (يجب تنظيفها)
- ابنِ **كاشف تلقائي** (`detectAdapter`) يفحص ترويسة الملف (header) ويختار المحوّل المناسب تلقائياً، مع رمي خطأ واضح عند الفشل ليمكن التعامل معه لاحقاً بخيار اختيار يدوي
- التصميم يجب أن يكون قابلاً للتوسعة: إضافة بنك جديد = ملف واحد جديد فقط في `src/adapters/`، بدون تعديل كود موجود

### 4.4 توحيد أسماء التجار (أصعب جزء تقني — انتبه له جيداً)
ابنِ في `src/core/merchantNormalizer.ts`:
- **دالة تنظيف** الوصف الخام: حذف أرقام المرجع، رموز المدن، النجوم `*`، الشرطات الزائدة، رموز نقاط البيع (POS)، وتوحيد الحالة (uppercase/lowercase) والمسافات
- **خوارزمية Levenshtein مطوّرة داخلياً من الصفر (بدون أي مكتبة خارجية)** لحساب مسافة التشابه بين وصفين
- **دالة تجميع (clustering)** تجمع الأوصاف المتشابهة في مجموعة تاجر واحدة، بعتبة تشابه قابلة للتعديل (اجعلها ثابتاً واضحاً في أعلى الملف يسهل معايرته لاحقاً)
- يجب الاحتفاظ بكل الأوصاف الأصلية داخل كل مجموعة (لعرضها للمستخدم لاحقاً للتحقق اليدوي)

---

## 5. الاختبارات المطلوبة في هذه الجلسة

اكتب اختبارات vitest لـ:
- كل محوّل من المحوّلات الأربعة (على الأقل 2–3 حالات لكل محوّل)
- الكاشف التلقائي للتنسيق
- تحليل التواريخ الغامضة (صيغ مختلفة، حالات حدية)
- توحيد أسماء التجار — **15 حالة اختبار على الأقل** (أوصاف مختلفة يجب أن تتجمع، وأوصاف مختلفة فعلاً يجب ألا تتجمع)
- حالات حدية: ملف CSV فارغ، ملف بترويسة فقط بدون بيانات، صف تالف/ناقص، ترميز غير UTF-8

الهدف: كل هذا الجزء يجب أن يمر بنجاح عبر `npm run test` قبل الانتقال للجزء التالي.

---

## 6. معايير القبول لهذه الجلسة تحديداً

- [ ] مشروع Vite+React+TS يعمل ويبني بدون أخطاء (`npm run build`)
- [ ] 4 محوّلات بنكية تعمل وتحوّل CSV خام إلى `Transaction[]` موحّد
- [ ] كاشف تلقائي للتنسيق يعمل
- [ ] توحيد أسماء التجار يعمل بخوارزمية Levenshtein محلية الصنع
- [ ] كل الاختبارات المذكورة أعلاه ناجحة
- [ ] commit أول نظيف على git بعد كل مرحلة فرعية (لا commit ضخم واحد في النهاية)
- [ ] **لا تكتب أي كود واجهة (UI/React components) في هذه الجلسة — فقط `src/core` و `src/adapters` والاختبارات**

---

## 7. ملاحظة مهمة لكلود كود

هذا الجزء الأول من ثلاثة. الجزء الثاني (محرك الكشف) والجزء الثالث (الواجهة + التقييم + النشر) سيُعطيان لك في جلسات منفصلة لاحقة، وسيعتمدان على المخرجات هنا. لذلك:
- التزم بالبنية المحددة تماماً (`src/core`, `src/adapters`) حتى تنسجم الجلسات القادمة مع عملك
- وثّق كل دالة تصديرها بتعليق قصير يشرح المدخلات والمخرجات، لأن الجلسة القادمة (بذاكرة مختلفة) ستحتاج تفهم الكود دون سياق سابق
